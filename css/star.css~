.main {
    display: inline-block;
}

.radio{
    position: absolute;
    left: 0;
    top: -50px;
}

.main:hover .star:hover, .main:hover .star:hover ~ .star, .radio:checked ~ .star {
    background-position: -6px 20px;
}

.star, .main:hover .star {
    float: right;
    display: block;
    width: 16px;
    height: 16px;
    background: url('../img/stars.jpg') -45px 20px;
}